<%= form_with(model: coding_class) do |form| %>
  <% if coding_class.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(coding_class.errors.count, "error") %> prohibited this coding_class from being saved:</h2>

      <ul>
        <% coding_class.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
